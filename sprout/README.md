# Sprout

A minimal Lisp interpreter written in Manta. Work in progress.

The AST is defined in `ast/`, with expression types for atoms, s-expressions, strings, numbers, and booleans. The `parser/` and `interp/` modules are stubs being built out incrementally.

## Run

```
manta run
```
