# Fibonacci

Computes the 80th Fibonacci number using an iterative algorithm and prints it to stdout.

Since Manta doesn't yet have a built-in integer-to-string conversion, `print_num` manually converts the result by finding the largest power of 10 that fits in the number, then stepping down through each digit position.

## Run

```
manta run
```
